#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: int that will be checked
 *
 * Return: 1 if is a digit, 0 if not
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

